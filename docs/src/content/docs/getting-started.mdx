---
title: Getting Started
description: Learn how to install and use Seizen DataTable in your React project.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { DemoPreview } from '@components/DemoPreview';

## Installation

<Tabs>
  <TabItem label="npm">
    ```bash
    npm install @izumisy/seizen-datatable-react
    ```
  </TabItem>
  <TabItem label="pnpm">
    ```bash
    pnpm add @izumisy/seizen-datatable-react
    ```
  </TabItem>
  <TabItem label="yarn">
    ```bash
    yarn add @izumisy/seizen-datatable-react
    ```
  </TabItem>
</Tabs>

## Quick Start

Here's a basic DataTable in action:

<DemoPreview demo="basic" height={300} client:load />

```tsx
import { useDataTable, DataTable } from "@izumisy/seizen-datatable-react";

const columns = [
  { accessorKey: "name", header: "Name" },
  { accessorKey: "age", header: "Age" },
  { accessorKey: "email", header: "Email" },
];

const data = [
  { name: "John Doe", age: 30, email: "john@example.com" },
  { name: "Jane Smith", age: 25, email: "jane@example.com" },
  { name: "Bob Wilson", age: 35, email: "bob@example.com" },
];

function BasicDemo() {
  const table = useDataTable({ data, columns });

  return <DataTable table={table} />;
}
```

## Next Steps

- Learn about the [Plugin System](/seizen-ui/guides/plugins/) to add features like filtering, row details, and more
- Explore [Data Adapters](/seizen-ui/reference/data-adapter/) for connecting to various data sources
- Check out [Theming](/seizen-ui/guides/theming/) to customize the appearance
